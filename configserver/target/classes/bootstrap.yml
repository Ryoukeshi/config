server:
    port: 8084

spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/Ryoukeshi/config-data.git
                    searchPaths: configs
                    default-label: "main"

    security:
        user:
            name: root
            password: s3cr3t